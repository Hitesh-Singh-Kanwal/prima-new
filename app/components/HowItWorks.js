export default function HowItWorks() {
  return (
    <section aria-labelledby="how-heading">
      <h2 id="how-heading" className="section-label">
        How it works
      </h2>

      <p className="how-body">
        <strong>
          PrimaLabs delivers a self-driving platform that maximizes AI infrastructure
          efficiency
        </strong>{" "}
        — closing the gap between what your hardware is capable of and what it actually
        delivers. The result is infrastructure that compounds in value over time — not a
        one-time tuning exercise, but a system that learns, improves, and stays ahead of
        performance drift automatically.
      </p>

      <hr className="divider" />
    </section>
  );
}
